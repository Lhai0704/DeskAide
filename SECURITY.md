# 安全政策

## 报告安全问题

请不要在公开 Issue 中提交可利用的漏洞、凭据或个人数据。目前仓库尚未启用 GitHub Private vulnerability reporting；可以先通过不含漏洞细节的 Issue 请求维护者提供私密联系渠道，取得渠道后再发送详情。如果 Security 页面日后提供“Report a vulnerability”，优先使用该私密入口。

## 安全原则

- API Key 不得写入源码、普通配置或日志。
- 不记录全局快捷键以外的键盘输入。
- 未经用户主动选择，不读取屏幕、窗口、剪贴板或浏览器内容。
- Tauri capability 保持最小权限。
- 平台未实现能力必须返回 `Unsupported`，不得伪造数据或静默降级为截图。

## 对话与语音数据

- 使用真实模型 Profile 时，用户发送的对话历史和本次明确选用的上下文会发送到该 Profile 配置的服务；内置 Mock Provider 不发送网络请求。
- API Key 保存在 Windows Credential Manager。普通设置与文字对话历史保存在本机应用数据目录；不要将这些文件、环境文件、日志或参考录音提交到 Git。
- 语音播报默认关闭。开启后，仅待朗读的助手回复片段和所选参考声音信息通过 Rust 后端发送到本机 `127.0.0.1:7860`，不直接向 TTS 发送用户提问、完整历史或窗口上下文。
- DeskAide 请求临时流式合成，不向 TTS 作品库保存音频、正文或生成元数据；参考音频预处理缓存仍留在 TTS 项目目录。DeskAide 原有的文字历史保存行为不变。
- 本地 TTS 服务只监听回环地址，保留来源检查；不为桌面接入放宽 WebView 的 CSP 或 TTS 的跨域策略。不要把该服务映射为公开网络接口。
- DeskAide 只终止自己启动的 TTS 进程；复用用户手动启动的服务时不接管其生命周期。

## Agent、工具与 MCP

- Context Registry 只存当前 turn 的显式选用内容，不持续采集，不跨会话注入，不实现 memory。
- 含桌面上下文的工具调用默认不保存参数/结果。批准执行和授权保存是两个独立决定，保存选项默认关闭、仅适用于该次调用；未授权正文在落盘前已经替换为省略标记。
- 无桌面上下文的工具参数/结果按正常结构化历史保存。助手回答中主动引用的内容仍随回答保存；历史是明文本地数据，后续继续该对话时会发给所选模型服务。
- 所有外部 MCP 工具逐次批准，不能凭工具名称或 server 的 readOnly annotation 自动获得信任。工具定义变更、切换会话或新 turn 会使旧授权失效。
- 本地程序以当前用户权限运行，批准不是系统沙箱。只启动可信可执行文件，使用 executable + args，不通过 shell 拼接；不自动安装 server。当前不支持自定义 env/secret env，不应把密钥填入普通参数。
- stdout 与 stderr 分离，协议帧、工具数量、Schema、参数和结果均有限制；不记录原始 stderr、模型请求正文、响应正文或工具参数/结果。Schema 不联网或读取文件解析外部引用。
- 停止生成可取消请求、批准和执行，但不能撤销外部副作用；中断/超时结果可能未知，不自动重试工具。
- Windows Job Object 追踪并清理 DeskAide 启动的 MCP 进程树；单 server 异常不应让应用或普通聊天失败。详细支持范围见 [MCP 安全与配置](docs/mcp.md)。
