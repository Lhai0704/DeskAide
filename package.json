{
  "name": "deskaide-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @deskaide/desktop",
    "tauri": "npm run tauri --workspace @deskaide/desktop --",
    "build": "npm run build --workspace @deskaide/desktop",
    "check": "npm run check --workspace @deskaide/desktop",
    "lint": "npm run lint --workspace @deskaide/desktop",
    "format": "npm run format --workspace @deskaide/desktop",
    "format:check": "npm run format:check --workspace @deskaide/desktop",
    "test": "npm run test --workspace @deskaide/desktop"
  },
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=11.0.0"
  }
}

